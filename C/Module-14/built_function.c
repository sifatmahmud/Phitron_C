#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    // double x;
    // scanf("%lf", &x);
    // int ans = ceil(x);
    // int ans = floor(x);
    // int ans = round(x);

    // double x;
    // scanf("%lf", &x);
    // double ans = sqrt(x);
    // printf("%0.2lf", ans);

    // int a, b;
    // scanf("%d %d", &a, &b);
    // int ans = pow(a, b);
    // printf("%d\n", ans);

    int a;
    scanf("%d", &a);
    int ans = abs(a);
    printf("%d\n", ans);
    return 0;
}